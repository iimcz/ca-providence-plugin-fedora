# ID of the dummy element which will only be used to upload a file, but not saved to CA. Should be of type media or file.
source_element_id = ..
# ID of the element where the resulting Fedora URL will be saved. Should be of type URL.
target_element_id = ..

fedora_repo = ...
collection_path = ...

# Login details for Fedora - these will be used via HTTP Basic Auth. 
username = ...
password = ...
